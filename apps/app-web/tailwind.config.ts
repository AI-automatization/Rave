// Tailwind v4 — barcha konfiguratsiya globals.css @theme / @plugin da.
// Bu fayl faqat editor tooling uchun saqlanadi.
import type { Config } from 'tailwindcss';

const config: Config = {
  content: ['./src/**/*.{js,ts,jsx,tsx,mdx}'],
};

export default config;
