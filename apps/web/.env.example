# CineSync Web — Environment Variables Example
# Bu faylni ko'chirib .env.local yarating:
#   cp apps/web/.env.example apps/web/.env.local
# Keyin bo'sh qiymatlarni to'ldiring.

# ─── Client-side (browser) ────────────────────────────────────────────────────
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_API_URL=http://localhost:3000
NEXT_PUBLIC_SOCKET_URL=http://localhost:3004

# ─── Server-side (Next.js API routes → backend services) ──────────────────────
AUTH_SERVICE_URL=http://localhost:3001/api/v1/auth

# ─── Auth ─────────────────────────────────────────────────────────────────────
NEXTAUTH_URL=http://localhost:3000
# Generatsiya: openssl rand -hex 32
NEXTAUTH_SECRET=

# ─── OAuth — Google Cloud Console dan olish kerak ─────────────────────────────
# https://console.cloud.google.com → APIs & Services → Credentials → OAuth 2.0
# Authorized redirect URIs: http://localhost:3000/api/auth/callback/google
NEXT_PUBLIC_GOOGLE_CLIENT_ID=
